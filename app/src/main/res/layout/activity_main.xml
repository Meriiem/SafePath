<?xml version="1.0" encoding="utf-8"?>
<!-- activity_main.xml -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="@color/background_color"
    tools:context="com.example.safepathproject.MainActivity">

    <!-- Logo and Title -->
    <ImageView
        android:id="@+id/logo"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_gravity="center_horizontal"
        android:src="@drawable/ic_safepath_logo"
        android:contentDescription="@string/logo_description" />

    <TextView
        android:id="@+id/app_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:text="@string/app_name"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@color/primary_text" />

    <!-- Buttons for Navigation -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginTop="20dp">

        <Button
            android:id="@+id/btn_map"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:drawableLeft="@drawable/ic_map"
            android:padding="16dp"
            android:drawablePadding="12dp"
            android:text="@string/safe_route_guidance"
            android:background="@drawable/button_background"
            android:textColor="@color/primary_text" />

        <Button
            android:id="@+id/btn_threat_detection"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:drawableLeft="@drawable/ic_threat_detection"
            android:padding="16dp"
            android:drawablePadding="12dp"
            android:text="@string/threat_detection"
            android:background="@drawable/button_background"
            android:textColor="@color/primary_text" />

        <Button
            android:id="@+id/btn_voice_activation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:drawableLeft="@drawable/ic_voice_activation"
            android:padding="16dp"
            android:drawablePadding="12dp"
            android:text="@string/voice_activation"
            android:background="@drawable/button_background"
            android:textColor="@color/primary_text" />

        <Button
            android:id="@+id/btn_settings"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:drawableLeft="@drawable/ic_settings"
            android:padding="16dp"
            android:drawablePadding="12dp"
            android:text="@string/settings"
            android:background="@drawable/button_background"
            android:textColor="@color/primary_text" />

        <Button
            android:id="@+id/btn_contacts"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:drawableLeft="@drawable/ic_contacts"
            android:padding="16dp"
            android:drawablePadding="12dp"
            android:text="@string/trusted_contacts"
            android:background="@drawable/button_background"
            android:textColor="@color/primary_text" />
    </LinearLayout>
</LinearLayout>
